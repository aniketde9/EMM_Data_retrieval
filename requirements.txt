# Python 3.10+. On 3.14, use Pillow 11.3+ (pinned below) for prebuilt wheels.
# Playwright removed: unused in this repo and pulls greenlet, which fails to build on 3.14.
fastapi==0.115.0
uvicorn==0.30.0
celery==5.4.0
redis==5.0.8
requests==2.32.3
beautifulsoup4==4.12.3
pillow==12.2.0
anthropic==0.40.0
python-dotenv==1.0.1
aiohttp==3.10.5
lxml==5.3.0
